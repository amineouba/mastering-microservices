apply plugin: "war"

bootWar {
    mainClass = "mosig.cloud.store.productorder.ProductorderApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
