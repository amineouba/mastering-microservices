apply plugin: "war"

bootWar {
    mainClass = "mosig.cloud.store.invoice.InvoiceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
